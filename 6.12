# Write a function called hypotenuse that returns the length of the hypotenuse of a right triangle,
# given the lengths of the two legs as parameters:


def hypotenuse(a,b):
	c = (a * a) + (b * b)
	c = c ** 0.5
	return c 


	
